{
  "name": "apotek-app",
  "version": "2.0.0",
  "type": "module",
  "description": "Aplikasi Manajemen Apotek dengan Supabase",
  "scripts": {
    "dev": "wrangler pages dev ./src --compatibility-date=2024-12-18",
    "build": "node build.js",
    "deploy": "npm run build && wrangler pages deploy dist",
    "supabase:init": "node setup-supabase.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}